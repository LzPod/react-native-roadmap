import {StyleSheet} from "react-native";
import COLORS from "../../../values/colors";

const styles = StyleSheet.create({
    tabBar: {
        backgroundColor: COLORS.blue.pale.medium,
        height: 70,
        padding: 20,
        position: "absolute",
    }
})

export default styles;